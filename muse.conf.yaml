- defaults:
    link:
      create: true
      relink: true
# - shell:
#     - rm -rf /tmp/yay
#     - git clone https://aur.archlinux.org/yay.git /tmp/yay
#     - cd /tmp/yay
#     - makepkg -si

- yay:
    - vi
