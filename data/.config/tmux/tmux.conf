# ================================ OPTIONS ================================
# fix colors in tmux
set -ga terminal-overrides ",xterm-256color:Tc"
# Let Neovim change cursor shape in terminal window # doesn't work
# set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[2 q'

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# Address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Increase scrollback buffer size from 2000 to 50000 lines
set -g history-limit 50000

# Increase tmux messages display duration from 750ms to 4s
set -g display-time 4000

# Refresh 'status-left' and 'status-right' more often, from every 15s to 5s
set -g status-interval 5

# Emacs key bindings in tmux command prompt (prefix + :) are better than
# vi keys, even for vim users
# set -g status-keys emacs

# Focus events enabled for terminals that support them
set -g focus-events on

# Super useful when using "grouped sessions" and multi-monitor setup
# setw -g aggressive-resize on

# don't rename windows automatically
# set -g allow-rename off

# Number window and panes starting with 1
set -g base-index 1                 # window index will start with 1
setw -g pane-base-index 1           # pane index will start with 1
set -g renumber-windows on

# Place status bar at top
set -g status-position top

# Transparent background
set -g status-style bg=default

# set -g status-format[0] "#[align=left range=left #{E:status-left-style}]#[push-default]#{T;=/#{status-left-length}:status-left}#[pop-default]#[norange default]#[list=on align=#{status-justify}]#[list=left-marker]<#[list=right-marker]>#[list=on]#{W:#[range=window|#{window_index} #{E:window-status-style}#{?#{&&:#{window_last_flag},#{!=:#{E:window-status-last-style},default}}, #{E:window-status-last-style},}#{?#{&&:#{window_bell_flag},#{!=:#{E:window-status-bell-style},default}}, #{E:window-status-bell-style},#{?#{&&:#{||:#{window_activity_flag},#{window_silence_flag}},#{!=:#{E:window-status-activity-style},default}}, #{E:window-status-activity-style},}}]#[push-default]#{T:window-statu-format}#[pop-default]#[norange default]#{?window_end_flag,,#{window-status-separator}},#[range=window|#{window_index} list=focus #{?#{!=:#{E:window-status-current-style},default},#{E:window-status-current-style},#{E:window-status-style}}#{?#{&&:#{window_last_flag},#{!=:#{E:window-status-last-style},default}}, #{E:window-status-last-style},}#{?#{&&:#{window_bell_flag},#{!=:#{E:window-status-bell-style},default}}, #{E:window-status-bell-style},#{?#{&&:#{||:#{window_activity_flag},#{window_silence_flag}},#{!=:#{E:window-status-activity-style},default}}, #{E:window-status-activity-style},}}]#[push-default]#{T:window-status-current-format}#[pop-default]#[norange list=on default]#{?window_end_flag,,#{window-status-separator}}}#[nolist align=right range=right #{E:status-right-style}]#[push-default]#{T;=/#{status-right-length}:status-right}#[pop-default]#[norange default]"
# set -g status-format[1] "#[align=centre]#{P:#{?pane_active,#[reverse],}#{pane_index}[#{pane_width}x#{pane_height}]#[default] }"
# set -g status-interval 5
# set -g status-justify left
# set -g status-keys vi
# set -g status-left "#[fg=#{?client_prefix,#f38ba8,#a6e3a1},bg=#1e1e2e,nobold,nounderscore,noitalics] #[fg=#1e1e2e,bg=#{?client_prefix,#f38ba8,#a6e3a1},nobold,nounderscore,noitalics] #[fg=#cdd6f4,bg=#313244] #S#[fg=#313244,bg=#1e1e2e,nobold,nounderscore,noitalics]"
# set -g status-left-length 100
# set -g status-left-style default
# set -g status-position top
# set -g status-right "#(/home/kevin/.config/tmux/plugins/tmux-continuum/scripts/continuum_save.sh)#[fg=#f5c2e7,bg=#1e1e2e,nobold,nounderscore,noitalics] #[fg=#1e1e2e,bg=#f5c2e7,nobold,nounderscore,noitalics] #[fg=#cdd6f4,bg=#313244] #W#[fg=#313244,bg=#1e1e2e,nobold,nounderscore,noitalics]#[fg=#BF616A,bg=#1e1e2e,nobold,nounderscore,noitalics] #[fg=#1e1e2e,bg=#BF616A,nobold,nounderscore,noitalics] #[fg=#cdd6f4,bg=#313244] #{cpu_percentage}#[fg=#313244,bg=#1e1e2e,nobold,nounderscore,noitalics]#[fg=#f9e2af,bg=#1e1e2e,nobold,nounderscore,noitalics] #[fg=#1e1e2e,bg=#f9e2af,nobold,nounderscore,noitalics] #[fg=#cdd6f4,bg=#313244] #{weather}#[fg=#313244,bg=#1e1e2e,nobold,nounderscore,noitalics]#[fg=#89b4fa,bg=#1e1e2e,nobold,nounderscore,noitalics] #[fg=#1e1e2e,bg=#89b4fa,nobold,nounderscore,noitalics]󰃰 #[fg=#cdd6f4,bg=#313244] %Y-%m-%d %H:%M#[fg=#313244,bg=#1e1e2e,nobold,nounderscore,noitalics]"
# set -g status-right-length 100
# set -g status-right-style default

# ================================ KEYBINDS ================================
# remap prefix from 'C-b' to 'C-a'
unbind C-b
set -g prefix C-a
bind-key C-a send-prefix

# reload config file (change file location to your the tmux.conf you want to use)
bind R source-file ~/.config/tmux/tmux.conf

# split panes using | and - to current directory
unbind %
bind-key "\\" split-window -h -c "#{pane_current_path}"
unbind '"'
bind-key - split-window -v -c "#{pane_current_path}"

# Easier and faster switching between next/prev window
bind C-p previous-window
bind C-n next-window
bind C-l last-window

# ================================ PLUGINS ================================
set -g @plugin 'tmux-plugins/tpm'

set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @resurrect-capture-pane-contents 'on'

set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @continuum-restore 'on'
# set -g status-right 'Continuum status: #{continuum_status}'

set -g @plugin 'christoomey/vim-tmux-navigator'

# set -g @plugin 'catppuccin/tmux'
# set -g @plugin 'tmux-plugins/tmux-cpu'
# set -g @plugin 'xamut/tmux-weather'

# set -g @tmux-weather-units "u"
# set -g @tmux-weather-location "NewOrleans"
# set -g @tmux-weather-format "%c+%t+%w+%l"

# set -g @catppuccin_window_current_fill "all"
# set -g @catppuccin_window_current_background "#2e3440" # text color
# set -g @catppuccin_window_current_color "#D08770"
# set -g @catppuccin_window_default_fill "none"
# set -g @catppuccin_window_default_color "#d8dee9" # text color
# # set -g @catppuccin_window_default_background "#000000"
# set -g @catppuccin_window_left_separator "█"
# set -g @catppuccin_window_middle_separator " | "
# set -g @catppuccin_window_right_separator "█ "
# set -g @catppuccin_window_status_enable "yes"
# set -g @catppuccin_icon_window_last "󰖰"
# set -g @catppuccin_icon_window_current " "
# set -g @catppuccin_icon_window_zoom "󰁌"
# set -g @catppuccin_icon_window_mark "󰃀"
# set -g @catppuccin_icon_window_silent "󰂛"
# set -g @catppuccin_icon_window_activity "󰖲"
# set -g @catppuccin_icon_window_bell "󰂞"

# set -g @catppuccin_status_modules_right "application cpu weather date_time"
# set -g @catppuccin_status_modules_left "session"
# set -g @catppuccin_status_left_separator  " "
# set -g @catppuccin_status_right_separator ""
# set -g @catppuccin_status_connect_separator "no"

# set -g @catppuccin_pane_active_border_style "fg=#81A1C1"

# set -g @catppuccin_cpu_color "#BF616A"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# need to run first!
# git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm
run '~/.config/tmux/plugins/tpm/tpm'
